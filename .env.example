# Local development / compose overrides
DATABASE_URL=postgresql://quota:quota@localhost:5432/quota
API_PORT=8000
DB_MIN_POOL_SIZE=2
DB_MAX_POOL_SIZE=16

# Used only by the pytest suite (admin DSN that may CREATE DATABASE)
TEST_DATABASE_URL=postgresql://postgres@localhost:5432/postgres
